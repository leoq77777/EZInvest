开发一个简单的辅助投资ai agent。底座大模型目前使用我本地基于ollama部署的gpt-oss-20b。这个agent利用rag工具，从akshare api获取最新的金融市场数据，并将数据存入分层存储结构（redis+mysql），当api中某些数据接口不够健康、受到限流时，能够通过冷热数据+数据低峰期预取来解决可用性问题。同时为该agent配置一个quant tool（使用强化学习、金融技术指标等，可以考虑现有的库，例如finRL）。同时配置一个政策分析tool，利用网络爬虫获取对应金融标的的新闻、政策文本，通过faiss存入数据库中，供后续使用。

端到端的工作流程：用户在前端（简历的web聊天框）输入投资问题，例如”最近的apple股票还能买吗“，大模型基于prompt对用户query进行扩展，再识别金融标的，并正确地从数据库获取该标的的数据，利用quant tool对数据进行量化计算；同时，模型利用政策tool从向量数据库中取出与该金融标的有关的新闻文本，结合新闻、政策、量化分析结果，为用户输出投资建议。

我已将akshare的网址提供给你访问: https://akshare.akfamily.xyz/tutorial.html

建议开发过程中，分part开发（敏捷开发），为每个部分配置对应的文档，每个部分开发完成后都执行单元测试，最后进行联调和端到端测试流程。